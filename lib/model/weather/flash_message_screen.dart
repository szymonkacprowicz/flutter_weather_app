import 'package:flutter/material.dart';

class ApiErrorMessage extends StatelessWidget {
  const ApiErrorMessage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
